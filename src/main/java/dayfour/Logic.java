package dayfour;


import java.util.ArrayList;
import java.util.List;

public class Logic {

/*  public static long calculateEx1(List<List<InputOutput.Type>> input, final int right, final int down) {
    int currentX = 0;
    int maxLength = input.get(0).size();
    List<InputOutput.Type> hits = new ArrayList<>();
    for (int y = down; y < input.size(); y = y+down) {
      List<InputOutput.Type> currentY = input.get(y);
      currentX = (currentX + right) % maxLength;
      hits.add(currentY.get(currentX));
    }
    return hits.stream().filter(item -> item.equals(InputOutput.Type.TREE)).count();
  }*/
}
